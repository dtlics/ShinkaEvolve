# NOTES.md — the orchestrator's per-run note (transient)

> **This is a SINGLE-RUN scratchpad, not a persistent ledger.** At the START of a run the
> orchestrator clears this file (the previous run's note is assumed already read and handled
> by the user). During and at the end of the run, write THIS run's note here: what happened,
> and anything the user must handle before the next run (e.g. a foundation/strategy fix you
> could not make mid-run). Persistent design teaching lives in
> `.claude/skills/shinka-orchestrator/SKILL.md`; CLAUDE.md is the standing operating guide;
> past runs' notes live in git history.

## This run
_(cleared — fill in during the run)_

## For the user to handle before the next run
_(none)_
