"""Vendored, frozen evaluation backbone for the pbb_code_discovery task.

Byte-faithful copy of the non-CSS (Campaign 5) construction + distance
pipeline from qiskit-community/qcode-discovery (Apache-2.0), with only
import paths rewritten (evaluation.* -> qcode_eval.*). Imported by the
Shinka evaluator; NOT part of the evolvable surface.
"""
